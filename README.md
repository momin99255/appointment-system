# Appointment System

Spring Boot 4.1.1 + Java 25 + MongoDB Atlas + JWT + Thymeleaf.

Roles: PATIENT, DOCTOR, ADMIN.

Set MONGODB_URI, JWT_SECRET, ADMIN_EMAIL and ADMIN_PASSWORD in the environment before running.
