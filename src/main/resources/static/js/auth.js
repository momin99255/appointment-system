async function post(url,data){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const text=await r.text();let d={};try{d=JSON.parse(text)}catch{}if(!r.ok)throw new Error(d.error||d.message||text||'Request failed');return d}
const loginForm=document.getElementById('loginForm');
if(loginForm){loginForm.addEventListener('submit',async e=>{e.preventDefault();const m=document.getElementById('message');m.textContent='';try{const d=await post('/api/auth/login',{email:document.getElementById('email').value,password:document.getElementById('password').value});localStorage.setItem('token',d.token);location.href='/dashboard'}catch(err){m.textContent=err.message}})}
const registerForm=document.getElementById('registerForm');
const role=document.getElementById('role');
if(role){role.addEventListener('change',()=>document.getElementById('doctorFields').classList.toggle('hidden',role.value!=='DOCTOR'))}
if(registerForm){registerForm.addEventListener('submit',async e=>{e.preventDefault();const m=document.getElementById('message');m.textContent='';const selectedRole=role?role.value:'PATIENT';const payload={name:document.getElementById('name').value,email:document.getElementById('email').value,password:document.getElementById('password').value,role:selectedRole};if(selectedRole==='DOCTOR'){payload.specialization=document.getElementById('specialization').value;payload.phone=document.getElementById('phone').value}try{const d=await post('/api/auth/register',payload);localStorage.setItem('token',d.token);location.href='/dashboard'}catch(err){m.textContent=err.message}})}
